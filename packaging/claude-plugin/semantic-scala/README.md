# semantic-scala Claude Code candidate

This source template is assembled with the exact published Alpha-3 MCPB. The
generated Claude Code plugin contains the canonical skill and a bundled local
stdio MCP server addressed through `${CLAUDE_PLUGIN_ROOT}`. It is limited to
Linux x86_64 with compatible GNU libc and system zlib and requires no host
Java.

This is a locally validated candidate, not a marketplace listing or submission.
