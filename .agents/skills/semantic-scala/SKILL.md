---
name: semantic-scala
description: Apply the repository's semantic-scala policy when Scala build truth or a concrete type, effect, symbol, reconciliation, or SemanticDB uncertainty could change the work.
---

# Codex wrapper

Read `../../../skills/semantic-scala/SKILL.md` completely, then follow it as
the authoritative policy. Invoke this repository skill explicitly as
`$semantic-scala` when needed; Codex may also select it from the description.
Do not duplicate or override the generic policy here.
